{
  "name": "tournament-contract",
  "version": "1.0.0",
  "description": "Tournament smart contract project",
  "scripts": {
    "start": "ganache-cli -h 0.0.0.0 --db ./data & sleep 5 && truffle migrate --network development",
    "test": "truffle test"
  },
  "dependencies": {
    "@truffle/hdwallet-provider": "^2.1.15",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "ganache-cli": "^6.12.2",
    "truffle": "^5.11.5"
  }
}
